const validateBody = require('./validateBody')
const isValidId = require('./isValidId')
const validateBodyFav = require('./validateBodyFav')
const authenticate = require('./authenticate')
const upload = require('./upload')

module.exports = {
  validateBody,
  isValidId,
  validateBodyFav,
  authenticate,
  upload,
};